/* const express = require ('express')
const app = express ()

app.get('/', (req, res) => { 
res.end ('olamundo')
})
app.listen (3000, () => console.log('controle')) 

 */

//const name = () => { pq não função? teve que declarar outra? 
//o que pega no nodeName é o valor do nó (elemento inteiro), que é o H1 ok
// element.innerText.includes ??? includes shildnodes os nós???
// o que é o packege.jason e suas linhas? p que é &&. etc?
// pacotes de instalação??? 
// padrão em ([{}])
try {
} catch (e) {
    console.log(e)
}
function funcao ()  {
    const element = document.getElementById('name')
   //const value = element.innerText.includes('A') ? alert(element.nodeName) : 'primeira ação' // ? e : seria um if else
   const value = element.innerText.includes('s') ? alert(element.innerText) : alert('não está contido')
  // console.log('value', element.innerText)
}